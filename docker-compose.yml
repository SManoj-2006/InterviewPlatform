version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: codecollab_mongo
    restart: unless-stopped
    ports:
      - "27018:27017"
    volumes:
      - mongo_data:/data/db

  piston:
    image: ghcr.io/engineer-man/piston
    container_name: codecollab_piston
    restart: unless-stopped
    privileged: true
    ports:
      - "2000:2000"
    volumes:
      - piston_data:/piston

  piston-init:
    image: curlimages/curl:8.10.1
    container_name: codecollab_piston_init
    depends_on:
      - piston
    restart: "no"
    entrypoint:
      - sh
      - -c
      - |
        set -e
        until curl -sf http://piston:2000/api/v2/packages >/dev/null; do
          echo "waiting for piston..."
          sleep 2
        done
        echo "installing piston runtimes..."
        curl -sS -X POST http://piston:2000/api/v2/packages -H "Content-Type: application/json" -d '{"language":"node","version":"18.15.0"}' >/dev/null
        curl -sS -X POST http://piston:2000/api/v2/packages -H "Content-Type: application/json" -d '{"language":"python","version":"3.10.0"}' >/dev/null
        curl -sS -X POST http://piston:2000/api/v2/packages -H "Content-Type: application/json" -d '{"language":"java","version":"15.0.2"}' >/dev/null
        echo "piston runtimes installed"

  backend:
    image: node:20-alpine
    container_name: codecollab_backend
    working_dir: /workspace/backend
    depends_on:
      - mongo
      - piston
      - piston-init
    env_file:
      - ./backend/.env
    environment:
      PORT: 3000
      NODE_ENV: development
      DB_URL: mongodb://mongo:27017/codecollab
      CLIENT_URL: http://192.168.1.102:5173
      # use host.docker.internal so the backend container can reach an existing
      # piston_api container that is bound to the host port 2000
      PISTON_API_URL: http://host.docker.internal:2000/api/v2
    ports:
      - "3000:3000"
    volumes:
      - .:/workspace
      - backend_node_modules:/workspace/backend/node_modules
    command: sh -c "if [ ! -x node_modules/.bin/nodemon ]; then npm install --no-audit --no-fund; fi; npm run dev"

  frontend:
    image: node:20-alpine
    container_name: codecollab_frontend
    working_dir: /workspace/frontend
    depends_on:
      - backend
    env_file:
      - ./frontend/.env
    environment:
      VITE_API_URL: http://192.168.1.102:3000/api
    ports:
      - "5173:5173"
    volumes:
      - .:/workspace
      - frontend_node_modules:/workspace/frontend/node_modules
    command: sh -c "if [ ! -x node_modules/.bin/vite ]; then npm install --no-audit --no-fund; fi; npm run dev -- --host 0.0.0.0 --port 5173"

volumes:
  mongo_data:
  piston_data:
  backend_node_modules:
  frontend_node_modules:
